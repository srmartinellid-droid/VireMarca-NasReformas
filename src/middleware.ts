import { createServerClient } from "@supabase/ssr";
import { NextResponse,type NextRequest } from "next/server";
const VERCEL_ALIAS="viremarca-nasreformas.vercel.app";const OFFICIAL_HOST="www.nascimentoreformas.com.br";
export async function middleware(request:NextRequest){if(request.nextUrl.hostname===VERCEL_ALIAS){const url=request.nextUrl.clone();url.protocol="https:";url.hostname=OFFICIAL_HOST;return NextResponse.redirect(url,301)}if(!request.nextUrl.pathname.startsWith("/admin"))return NextResponse.next();let response=NextResponse.next();const supabase=createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,{cookies:{getAll(){return request.cookies.getAll()},setAll(cookiesToSet){cookiesToSet.forEach(({name,value})=>request.cookies.set(name,value));response=NextResponse.next({request});cookiesToSet.forEach(({name,value,options})=>response.cookies.set(name,value,options))}}});await supabase.auth.getUser();const requestHeaders=new Headers(request.headers);requestHeaders.set("x-nas-admin-path",request.nextUrl.pathname);response=NextResponse.next({request:{headers:requestHeaders}});return response}
export const config={matcher:["/((?!_next/static|_next/image|favicon.ico|icon.svg|apple-icon|.*\\.(?:png|jpg|jpeg|gif|webp|svg|ico|css|js|map|txt|xml|woff2?)$).*)"]};
