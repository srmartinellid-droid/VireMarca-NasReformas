"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { createClient } from "@/lib/supabase/client";
const keys=["site_name","whatsapp","region","accent_color","seo_title","seo_description","logo_url","favicon_url"];
const defaults={site_name:"Nascimento Reformas",whatsapp:"554892056761",region:"Palhoça e Grande Florianópolis - SC",accent_color:"#245675",seo_title:"Nascimento Reformas | Reformas na Grande Florianópolis",seo_description:"Reformas residenciais e comerciais com precisão, planejamento e acabamento.",logo_url:"/logo.png",favicon_url:"/logo.png"};
export default function AdminConfigPage(){const [values,setValues]=useState<Record<string,string>>(defaults);const [message,setMessage]=useState("");const [saving,setSaving]=useState(false);const [uploading,setUploading]=useState(false);useEffect(()=>{(async()=>{const s=createClient();const {data}=await s.from("settings").select("key,value").in("key",keys);for(const row of data??[])setValues(v=>({...v,[row.key]:row.value||defaults[row.key as keyof typeof defaults]||""}))})()},[]);
async function save(){setSaving(true);setMessage("");const s=createClient();for(const key of keys){const {error}=await s.from("settings").upsert({key,value:values[key]||"",updated_at:new Date().toISOString()},{onConflict:"key"});if(error){setMessage(`Erro: ${error.message}`);setSaving(false);return}}setMessage("Configurações salvas.");setSaving(false)}
async function uploadBrand(file:File){setUploading(true);setMessage("");const s=createClient();const ext=file.name.split(".").pop()?.toLowerCase()||"png";const path=`brand/${crypto.randomUUID()}.${ext}`;const {error}=await s.storage.from("site-media").upload(path,file,{contentType:file.type,upsert:true});if(error){setMessage(`Upload: ${error.message}`);setUploading(false);return}const {data}=s.storage.from("site-media").getPublicUrl(path);const url=data.publicUrl;setValues(v=>({...v,logo_url:url,favicon_url:url}));const writes=await Promise.all([s.from("settings").upsert({key:"logo_url",value:url,updated_at:new Date().toISOString()},{onConflict:"key"}),s.from("settings").upsert({key:"favicon_url",value:url,updated_at:new Date().toISOString()},{onConflict:"key"})]);const failed=writes.find(r=>r.error);setMessage(failed?`Upload feito, mas não foi possível salvar: ${failed.error?.message}`:"Logotipo e favicon atualizados.");setUploading(false)}
return <div className="min-h-screen bg-stone-100"><header className="border-b border-stone-200 bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5"><div><Link href="/admin/dashboard" className="text-xs uppercase tracking-widest text-stone-500">← Dashboard</Link><h1 className="mt-1 text-xl font-semibold text-navy-950">Configurações</h1></div><button onClick={save} disabled={saving} className="rounded-lg bg-navy-900 px-5 py-2.5 text-sm font-semibold text-white">{saving?"Salvando…":"Salvar"}</button></div></header><main className="mx-auto max-w-5xl px-5 py-8">{message&&<div className="mb-6 border border-stone-200 bg-white p-3 text-sm text-navy-900">{message}</div>}<section className="bg-white p-6 sm:p-8"><h2 className="text-xl font-semibold text-navy-950">Identidade da marca</h2><p className="mt-1 text-sm text-stone-500">O mesmo arquivo é usado no cabeçalho e como favicon. PNG, JPG, WebP ou SVG.</p><div className="mt-6 grid gap-6 md:grid-cols-[220px_1fr] md:items-center"><div className="flex min-h-40 items-center justify-center border border-stone-200 bg-stone-50 p-5"><img src={values.logo_url||"/logo.png"} alt="Logotipo atual" className="max-h-28 max-w-full object-contain"/></div><div><label className="inline-flex cursor-pointer rounded-lg bg-navy-900 px-5 py-3 text-sm font-semibold text-white">{uploading?"Enviando…":"Trocar logotipo"}<input type="file" accept="image/png,image/jpeg,image/webp,image/svg+xml" className="hidden" disabled={uploading} onChange={e=>e.target.files?.[0]&&uploadBrand(e.target.files[0])}/></label><p className="mt-3 text-xs leading-relaxed text-stone-500">Ao enviar um novo logotipo, o painel salva automaticamente o endereço da imagem e atualiza o favicon.</p></div></div></section><section className="mt-7 bg-white p-6 sm:p-8"><h2 className="text-xl font-semibold text-navy-950">Identidade e contato</h2><div className="mt-6 grid gap-5 sm:grid-cols-2"><Input label="Nome do site" value={values.site_name} set={v=>setValues(x=>({...x,site_name:v}))}/><Input label="WhatsApp (somente números)" value={values.whatsapp} set={v=>setValues(x=>({...x,whatsapp:v.replace(/\D/g,"")}))}/><Input label="Região" value={values.region} set={v=>setValues(x=>({...x,region:v}))}/><Input label="Cor de destaque" type="color" value={values.accent_color} set={v=>setValues(x=>({...x,accent_color:v}))}/></div></section><section className="mt-7 bg-white p-6 sm:p-8"><h2 className="text-xl font-semibold text-navy-950">SEO</h2><div className="mt-6 grid gap-5"><Input label="Título SEO" value={values.seo_title} set={v=>setValues(x=>({...x,seo_title:v}))}/><label><span className="mb-2 block text-xs font-semibold uppercase tracking-wider text-stone-500">Descrição SEO</span><textarea rows={4} value={values.seo_description} onChange={e=>setValues(x=>({...x,seo_description:e.target.value}))} className="w-full rounded-lg border border-stone-300 p-3 text-sm"/></label></div></section><div className="py-8"><button onClick={save} disabled={saving} className="rounded-lg bg-navy-900 px-6 py-3 text-sm font-semibold text-white">{saving?"Salvando…":"Salvar configurações"}</button></div></main></div>}
function Input({label,value,set,type="text"}:{label:string;value:string;set:(v:string)=>void;type?:string}){return <label><span className="mb-2 block text-xs font-semibold uppercase tracking-wider text-stone-500">{label}</span><input type={type} value={value} onChange={e=>set(e.target.value)} className="h-11 w-full rounded-lg border border-stone-300 px-3 text-sm"/></label>}
