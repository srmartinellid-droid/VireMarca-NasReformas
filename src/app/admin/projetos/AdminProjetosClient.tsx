"use client";
import Link from "next/link";import{useState}from"react";import{createClient}from"@/lib/supabase/client";
export type Project={id:number;slug:string;title:string;description:string|null;category_id:number|null;location:string|null;year:number|null;cover_image:string|null;featured:boolean;published:boolean;display_order:number};export type Category={id:number;name:string};const blank:Omit<Project,"id">={slug:"",title:"",description:"",category_id:null,location:"",year:new Date().getFullYear(),cover_image:"",featured:false,published:false,display_order:0};
export default function AdminProjetosClient({initialProjects,initialCategories}:{initialProjects:Project[];initialCategories:Category[]}){const[projects,setProjects]=useState(initialProjects);const[categories]=useState(initialCategories);const[form,setForm]=useState<Omit<Project,"id">>(blank);const[editing,setEditing]=useState<number|null>(null);const[saving,setSaving]=useState(false);const[uploading,setUploading]=useState(false);const[message,setMessage]=useState("");
function edit(p:Project){const{id,...rest}=p;setEditing(id);setForm(rest);window.scrollTo({top:0,behavior:"smooth"})}function reset(){setEditing(null);setForm(blank)}
async function save(){setSaving(true);setMessage("");const s=createClient();const payload={...form,slug:form.slug.trim().toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"")};const result=editing?await s.from("projects").update(payload).eq("id",editing):await s.from("projects").insert(payload).select("id").single();if(result.error)setMessage(result.error.message);else{setMessage("Projeto salvo.");if(editing)setProjects(rows=>rows.map(row=>row.id===editing?{...row,id:editing,...payload}:row));else if(result.data){setProjects(rows=>[...rows,{id:result.data.id,...payload} as Project]);window.location.href="/admin/projetos/"+result.data.id}}setSaving(false)}
async function remove(id:number){if(!window.confirm("Excluir este projeto?"))return;const s=createClient();const{error}=await s.from("projects").delete().eq("id",id);if(error)setMessage(error.message);else setProjects(rows=>rows.filter(row=>row.id!==id))}
async function uploadCover(file:File){setUploading(true);const s=createClient();const ext=file.name.split(".").pop()?.toLowerCase()||"jpg";const path="projects/"+crypto.randomUUID()+"."+ext;const{error}=await s.storage.from("site-media").upload(path,file,{contentType:file.type});if(error)setMessage(error.message);else{const{data}=s.storage.from("site-media").getPublicUrl(path);setForm(f=>({...f,cover_image:data.publicUrl}))}setUploading(false)}
return <div className="min-h-screen bg-stone-100"><header className="sticky top-0 z-20 border-b border-stone-200 bg-white/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><div><Link href="/admin/dashboard" className="text-xs uppercase tracking-widest text-stone-500">← Dashboard</Link><h1 className="mt-1 text-xl font-semibold text-navy-950">Portfólio</h1></div><Link href="/" className="text-sm font-medium text-navy-700">Ver site ↗</Link></div></header><main className="mx-auto max-w-7xl space-y-8 px-5 py-8">{message&&<div className="border border-stone-200 bg-white p-3 text-sm text-navy-900">{message}</div>}<section className="border border-stone-200 bg-white p-6 sm:p-8"><div className="mb-7 flex items-start justify-between border-b border-stone-100 pb-5"><div><h2 className="text-xl font-semibold text-navy-950">{editing?"Editar projeto":"Novo projeto"}</h2><p className="mt-1 text-sm text-stone-500">Capa, descrição, categoria, ordem e publicação.</p></div>{editing&&<button onClick={reset} className="text-sm text-stone-500 underline">Cancelar edição</button>}</div><div className="grid gap-5 lg:grid-cols-2"><Input label="Título" value={form.title} onChange={v=>setForm(f=>({...f,title:v}))}/><Input label="Slug" value={form.slug} onChange={v=>setForm(f=>({...f,slug:v}))}/><Input label="Localização" value={form.location||""} onChange={v=>setForm(f=>({...f,location:v}))}/><Input label="Ano" type="number" value={String(form.year||"")} onChange={v=>setForm(f=>({...f,year:Number(v)||null}))}/><Select label="Categoria" value={String(form.category_id||"")} onChange={v=>setForm(f=>({...f,category_id:v?Number(v):null}))} options={categories.map(c=>[String(c.id),c.name])}/><Input label="Ordem" type="number" value={String(form.display_order)} onChange={v=>setForm(f=>({...f,display_order:Number(v)||0}))}/><label className="lg:col-span-2"><span className="mb-2 block text-xs font-semibold uppercase tracking-wider text-stone-500">Descrição</span><textarea rows={4} value={form.description||""} onChange={e=>setForm(f=>({...f,description:e.target.value}))} className="w-full rounded-lg border border-stone-300 p-3"/></label></div><div className="mt-6 flex flex-wrap gap-6"><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.featured} onChange={e=>setForm(f=>({...f,featured:e.target.checked}))}/> Destaque</label><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.published} onChange={e=>setForm(f=>({...f,published:e.target.checked}))}/> Publicado</label></div><div className="mt-7 border-t border-stone-100 pt-6"><div className="mb-3 flex items-center justify-between"><p className="text-sm font-semibold text-navy-900">Imagem de capa</p><label className="cursor-pointer rounded-lg border border-navy-900 px-4 py-2 text-sm font-semibold text-navy-900">{uploading?"Enviando…":"Enviar capa"}<input type="file" accept="image/*" className="hidden" onChange={e=>e.target.files?.[0]&&void uploadCover(e.target.files[0])}/></label></div>{form.cover_image&&<img src={form.cover_image} alt="Prévia da capa" className="aspect-[16/7] max-h-80 w-full object-cover"/>}</div><button onClick={()=>void save()} disabled={saving} className="mt-7 rounded-lg bg-navy-900 px-6 py-3 text-sm font-semibold text-white disabled:opacity-50">{saving?"Salvando…":editing?"Salvar projeto":"Publicar projeto"}</button></section><section className="overflow-hidden border border-stone-200 bg-white"><div className="border-b border-stone-100 px-6 py-5"><h2 className="font-semibold text-navy-950">Projetos cadastrados</h2></div><div className="divide-y divide-stone-100">{projects.map(p=><article key={p.id} className="grid gap-4 p-5 md:grid-cols-[140px_1fr_auto] md:items-center"><div className="aspect-[4/3] overflow-hidden bg-stone-100">{p.cover_image?<img src={p.cover_image} alt="" className="h-full w-full object-cover"/>:<div className="grid h-full place-items-center text-xs text-stone-400">Sem capa</div>}</div><div><h3 className="font-semibold text-navy-900">{p.title}</h3><p className="mt-1 text-sm text-stone-500">{p.location||"Sem localização"}{p.year?" · "+p.year:""}</p></div><div className="flex flex-wrap gap-2"><Link href={"/admin/projetos/"+p.id} className="rounded border border-stone-300 px-3 py-2 text-sm">Galeria</Link><button onClick={()=>edit(p)} className="rounded border border-stone-300 px-3 py-2 text-sm">Editar</button><button onClick={()=>void remove(p.id)} className="rounded border border-red-200 px-3 py-2 text-sm text-red-600">Excluir</button></div></article>)}</div></section></main></div>}
function Input({label,value,onChange,type="text"}:{label:string;value:string;onChange:(v:string)=>void;type?:string}){return <label><span className="mb-2 block text-xs font-semibold uppercase tracking-wider text-stone-500">{label}</span><input type={type} value={value} onChange={e=>onChange(e.target.value)} className="h-11 w-full rounded-lg border border-stone-300 px-3"/></label>}
function Select({label,value,onChange,options}:{label:string;value:string;onChange:(v:string)=>void;options:string[][]}){return <label><span className="mb-2 block text-xs font-semibold uppercase tracking-wider text-stone-500">{label}</span><select value={value} onChange={e=>onChange(e.target.value)} className="h-11 w-full rounded-lg border border-stone-300 bg-white px-3"><option value="">Sem categoria</option>{options.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label>}
