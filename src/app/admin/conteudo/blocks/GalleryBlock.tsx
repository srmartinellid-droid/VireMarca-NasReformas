import type { HomepageContent,GalleryImage } from "@/lib/cms-defaults";
export default function GalleryBlock({content,setContent,uploading,onUpload,onRemove}:{content:HomepageContent["gallery"];setContent:(value:HomepageContent["gallery"])=>void;uploading:string;onUpload:(file:File,index?:number)=>void;onRemove:(index:number)=>void}){function update(index:number,changes:Partial<GalleryImage>){setContent({...content,images:content.images.map((item,i)=>i===index?{...item,...changes}:item)})}return <section className="border border-stone-200 bg-white p-6 shadow-sm sm:p-8"><div className="mb-6 border-b border-stone-100 pb-5"><p className="text-xs font-bold uppercase tracking-[.2em] text-accent-600">03 · Galeria</p><h2 className="mt-1 text-xl font-semibold text-navy-950">Galeria horizontal</h2></div><div className="grid gap-5 lg:grid-cols-2"><Input label="Eyebrow" value={content.eyebrow} onChange={v=>setContent({...content,eyebrow:v})}/><Input label="Título" value={content.title} onChange={v=>setContent({...content,title:v})}/><Input label="Intervalo (ms)" type="number" value={String(content.interval)} onChange={v=>setContent({...content,interval:Math.max(3500,Number(v)||6200)})}/></div><div className="mt-6 flex items-center justify-between border-t border-stone-100 pt-5"><p className="text-sm font-semibold text-navy-900">{content.images.length} imagens</p><label className="cursor-pointer rounded-lg bg-navy-900 px-4 py-2.5 text-xs font-semibold text-white">{uploading==="gallery-new"?"Enviando…":"Adicionar imagem"}<input type="file" accept="image/*" className="hidden" disabled={!!uploading} onChange={e=>{const f=e.target.files?.[0];if(f)onUpload(f);e.currentTarget.value=""}}/></label></div><div className="mt-5 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{content.images.map((item,index)=><article key={item.image+index} className="overflow-hidden border border-stone-200 bg-stone-50"><img src={item.image} alt={item.alt||""} className="aspect-[4/3] w-full object-cover"/><div className="grid gap-2 p-3"><Input label="Legenda" value={item.label||""} onChange={v=>update(index,{label:v})}/><Input label="Texto alternativo" value={item.alt||""} onChange={v=>update(index,{alt:v})}/><div className="flex justify-between pt-1 text-xs"><label className="cursor-pointer font-semibold text-navy-900">{uploading==="gallery-"+index?"Enviando…":"Trocar"}<input type="file" accept="image/*" className="hidden" disabled={!!uploading} onChange={e=>{const f=e.target.files?.[0];if(f)onUpload(f,index);e.currentTarget.value=""}}/></label><button onClick={()=>onRemove(index)} className="font-semibold text-red-600">Remover</button></div></div></article>)}</div><p className="mt-4 text-xs text-stone-500">Legendas e textos alternativos ficam no estado local até Salvar alterações.</p></section>}
function Input({label,value,onChange,type="text"}:{label:string;value:string;onChange:(v:string)=>void;type?:string}){return <label><span className="mb-1 block text-[10px] font-bold uppercase tracking-wider text-stone-500">{label}</span><input type={type} value={value} onChange={e=>onChange(e.target.value)} className="h-10 w-full rounded-lg border border-stone-300 px-3 text-sm"/></label>}
