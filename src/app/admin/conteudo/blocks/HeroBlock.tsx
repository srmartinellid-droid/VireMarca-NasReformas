import type { HomepageContent } from "@/lib/cms-defaults";
export default function HeroBlock({content,setContent,uploading,onUpload,onRemove}:{content:HomepageContent["hero"];setContent:(value:HomepageContent["hero"])=>void;uploading:string;onUpload:(file:File,index?:number)=>void;onRemove:(index:number)=>void}){return <section className="border border-stone-200 bg-white p-6 shadow-sm sm:p-8"><div className="mb-6 border-b border-stone-100 pb-5"><p className="text-xs font-bold uppercase tracking-[.2em] text-accent-600">01 · Hero</p><h2 className="mt-1 text-xl font-semibold text-navy-950">Primeira impressão</h2></div><div className="grid gap-5 lg:grid-cols-2"><Input label="Eyebrow" value={content.eyebrow} onChange={v=>setContent({...content,eyebrow:v})}/><Input label="Título" value={content.title} onChange={v=>setContent({...content,title:v})}/><Input label="Destaque" value={content.highlight} onChange={v=>setContent({...content,highlight:v})}/><Input label="Intervalo (ms)" type="number" value={String(content.heroInterval)} onChange={v=>setContent({...content,heroInterval:Math.max(2500,Number(v)||6500)})}/><TextArea label="Descrição" value={content.description} onChange={v=>setContent({...content,description:v})}/><TextArea label="Texto do card" value={content.panelText} onChange={v=>setContent({...content,panelText:v})}/></div><label className="mt-5 flex items-center gap-2 text-sm"><input type="checkbox" checked={content.showGrid} onChange={e=>setContent({...content,showGrid:e.target.checked})}/> Mostrar grade arquitetônica</label><ImageManager images={content.images} uploading={uploading} onUpload={onUpload} onRemove={onRemove}/></section>}
function Input({label,value,onChange,type="text"}:{label:string;value:string;onChange:(v:string)=>void;type?:string}){return <label><span className="mb-2 block text-xs font-semibold uppercase tracking-wider text-stone-500">{label}</span><input type={type} value={value} onChange={e=>onChange(e.target.value)} className="h-11 w-full rounded-lg border border-stone-300 px-3"/></label>}
function TextArea({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <label><span className="mb-2 block text-xs font-semibold uppercase tracking-wider text-stone-500">{label}</span><textarea rows={4} value={value} onChange={e=>onChange(e.target.value)} className="w-full rounded-lg border border-stone-300 p-3"/></label>}
function ImageManager({images,uploading,onUpload,onRemove}:{images:{image:string;alt?:string}[];uploading:string;onUpload:(file:File,index?:number)=>void;onRemove:(index:number)=>void}){return <div className="mt-7 border-t border-stone-100 pt-6"><div className="flex items-center justify-between"><p className="text-sm font-semibold text-navy-900">Imagens</p><label className="cursor-pointer rounded-lg bg-navy-900 px-4 py-2.5 text-xs font-semibold text-white">{uploading==="hero-new"?"Enviando…":"Adicionar imagem"}<input type="file" accept="image/*" className="hidden" disabled={!!uploading} onChange={e=>{const f=e.target.files?.[0];if(f)onUpload(f);e.currentTarget.value=""}}/></label></div><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{images.map((item,index)=><div key={item.image+index} className="overflow-hidden border border-stone-200"><img src={item.image} alt={item.alt||""} className="aspect-[4/3] w-full object-cover"/><div className="flex justify-between p-3 text-xs"><label className="cursor-pointer font-semibold text-navy-900">{uploading==="hero-"+index?"Enviando…":"Trocar"}<input type="file" accept="image/*" className="hidden" disabled={!!uploading} onChange={e=>{const f=e.target.files?.[0];if(f)onUpload(f,index);e.currentTarget.value=""}}/></label><button onClick={()=>onRemove(index)} className="font-semibold text-red-600">Remover</button></div></div>)}</div></div>}
