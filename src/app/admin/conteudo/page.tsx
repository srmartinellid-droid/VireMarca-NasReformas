"use client";
import Link from "next/link";
import { useEffect, useState, type ReactNode } from "react";
import { createClient } from "@/lib/supabase/client";
import { DEFAULT_HOMEPAGE, type HomepageContent } from "@/lib/cms-defaults";

export default function AdminConteudoPage(){
 const [content,setContent]=useState<HomepageContent>(DEFAULT_HOMEPAGE);const [loading,setLoading]=useState(true);const [saving,setSaving]=useState(false);const [message,setMessage]=useState("");const [uploading,setUploading]=useState(false);
 useEffect(()=>{(async()=>{const s=createClient();const {data,error}=await s.from("site_content").select("value").eq("key","homepage").maybeSingle();if(error)setMessage(`Erro ao carregar: ${error.message}`);if(data?.value){const v=data.value as Partial<HomepageContent>;setContent({...DEFAULT_HOMEPAGE,...v,hero:{...DEFAULT_HOMEPAGE.hero,...(v.hero??{})},areas:{...DEFAULT_HOMEPAGE.areas,...(v.areas??{})},portfolio:{...DEFAULT_HOMEPAGE.portfolio,...(v.portfolio??{})},method:{...DEFAULT_HOMEPAGE.method,...(v.method??{})},finalCta:{...DEFAULT_HOMEPAGE.finalCta,...(v.finalCta??{})}})}setLoading(false)})()},[]);
 async function persist(next:HomepageContent){const s=createClient();return s.from("site_content").upsert({key:"homepage",value:next,updated_at:new Date().toISOString()},{onConflict:"key"});}
 async function save(){setSaving(true);setMessage("");const {error}=await persist(content);setMessage(error?`Não foi possível salvar: ${error.message}`:"Conteúdo salvo e publicado.");setSaving(false)}
 async function uploadHero(file:File){setUploading(true);setMessage("");const s=createClient();const ext=file.name.split(".").pop()?.toLowerCase()||"jpg";const path=`hero/${crypto.randomUUID()}.${ext}`;const up=await s.storage.from("site-media").upload(path,file,{contentType:file.type,upsert:false});if(up.error){setMessage(`Upload: ${up.error.message}`);setUploading(false);return}const {data}=s.storage.from("site-media").getPublicUrl(path);const next={...content,hero:{...content.hero,images:[...content.hero.images,{image:data.publicUrl,alt:"Obra Nascimento Reformas"}]}};setContent(next);const saved=await persist(next);setMessage(saved.error?`Imagem enviada, mas não salva: ${saved.error.message}`:"Imagem da hero enviada e salva.");setUploading(false)}
 function heroField(key:string,value:string){setContent(c=>({...c,hero:{...c.hero,[key]:value}} as HomepageContent))}
 function field(section:keyof HomepageContent,key:string,value:string){setContent(c=>({...c,[section]:{...(c[section] as object),[key]:value}} as HomepageContent))}
 if(loading)return <div className="min-h-screen bg-stone-100 p-8 text-stone-600">Carregando conteúdo…</div>;
 return <div className="min-h-screen bg-stone-100"><header className="sticky top-0 z-20 border-b border-stone-200 bg-white/90 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><div><Link href="/admin/dashboard" className="text-xs uppercase tracking-widest text-stone-500">← Dashboard</Link><h1 className="mt-1 text-xl font-semibold text-navy-950">Editor do site</h1></div><button onClick={save} disabled={saving} className="rounded-lg bg-navy-900 px-5 py-2.5 text-sm font-semibold text-white">{saving?"Salvando…":"Salvar alterações"}</button></div></header><main className="mx-auto max-w-7xl space-y-8 px-5 py-8">{message&&<div className="border border-stone-200 bg-white px-4 py-3 text-sm text-navy-900">{message}</div>}
 <EditorCard title="Hero · primeira impressão" subtitle="Texto, destaque, cores, degradê, grade e imagens são editáveis."><div className="grid gap-5 lg:grid-cols-2"><Input label="Eyebrow" value={content.hero.eyebrow} onChange={v=>heroField("eyebrow",v)}/><Input label="Título principal" value={content.hero.title} onChange={v=>heroField("title",v)}/><Input label="Texto que recebe destaque" value={content.hero.highlight} onChange={v=>heroField("highlight",v)}/><ColorInput label="Cor do destaque da hero" value={content.hero.accentColor} onChange={v=>heroField("accentColor",v)}/><TextArea label="Descrição" value={content.hero.description} onChange={v=>heroField("description",v)}/><Input label="Título do card" value={content.hero.panelTitle} onChange={v=>heroField("panelTitle",v)}/><TextArea label="Texto do card" value={content.hero.panelText} onChange={v=>heroField("panelText",v)}/><div className="grid grid-cols-2 gap-4"><Input label="Intervalo (ms)" value={String(content.hero.heroInterval)} onChange={v=>setContent(c=>({...c,hero:{...c.hero,heroInterval:Math.max(2500,Number(v)||6500)}}))} type="number"/><Input label="Degradê / overlay (%)" value={String(content.hero.heroOverlay)} onChange={v=>setContent(c=>({...c,hero:{...c.hero,heroOverlay:Math.min(90,Math.max(0,Number(v)||0))}}))} type="number"/></div></div><div className="mt-5 flex flex-wrap gap-6 border-t border-stone-100 pt-5"><label className="flex items-center gap-2 text-sm font-medium text-navy-900"><input type="checkbox" checked={content.hero.showGrid} onChange={e=>setContent(c=>({...c,hero:{...c.hero,showGrid:e.target.checked}}))}/> Mostrar grade arquitetônica na hero</label></div><div className="mt-7 border-t border-stone-200 pt-7"><div className="flex items-center justify-between"><div><h3 className="font-semibold text-navy-900">Imagens da hero</h3><p className="text-sm text-stone-500">Cada upload é salvo no banco imediatamente.</p></div><label className="cursor-pointer rounded-lg border border-navy-900 px-4 py-2 text-sm font-semibold text-navy-900">{uploading?"Enviando…":"Adicionar imagem"}<input type="file" accept="image/*" className="hidden" disabled={uploading} onChange={e=>e.target.files?.[0]&&uploadHero(e.target.files[0])}/></label></div><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{content.hero.images.map((image,index)=><div key={`${image.image}-${index}`} className="group relative overflow-hidden border border-stone-200 bg-stone-200"><img src={image.image} alt={image.alt||""} className="aspect-[4/3] w-full object-cover"/><button onClick={async()=>{const next={...content,hero:{...content.hero,images:content.hero.images.filter((_,i)=>i!==index)}};setContent(next);const r=await persist(next);setMessage(r.error?`Erro ao remover: ${r.error.message}`:"Imagem removida e salvo.")} } className="absolute right-2 top-2 rounded bg-black/70 px-2 py-1 text-xs text-white opacity-0 transition group-hover:opacity-100">Remover</button><div className="p-2 text-xs text-stone-500">Slide {String(index+1).padStart(2,"0")}</div></div>)}</div></div></EditorCard>
 <EditorCard title="Áreas de atuação" subtitle="Título, destaque e descrição da seção."><div className="grid gap-5 lg:grid-cols-2"><Input label="Eyebrow" value={content.areas.eyebrow} onChange={v=>field("areas","eyebrow",v)}/><Input label="Título" value={content.areas.title} onChange={v=>field("areas","title",v)}/><Input label="Título em destaque" value={content.areas.highlight} onChange={v=>field("areas","highlight",v)}/><TextArea label="Descrição" value={content.areas.description} onChange={v=>field("areas","description",v)}/></div></EditorCard>
 <EditorCard title="Portfólio / galeria" subtitle="A home usa os projetos publicados. A gestão das fotos fica em Portfólio no painel."><div className="grid gap-5 lg:grid-cols-2"><Input label="Eyebrow" value={content.portfolio.eyebrow} onChange={v=>field("portfolio","eyebrow",v)}/><Input label="Título" value={content.portfolio.title} onChange={v=>field("portfolio","title",v)}/><TextArea label="Descrição" value={content.portfolio.description} onChange={v=>field("portfolio","description",v)}/><Input label="Título quando vazio" value={content.portfolio.emptyTitle} onChange={v=>field("portfolio","emptyTitle",v)}/><TextArea label="Mensagem quando vazio" value={content.portfolio.emptyText} onChange={v=>field("portfolio","emptyText",v)}/></div></EditorCard>
 <EditorCard title="Método" subtitle="Seção de processo e confiança."><div className="grid gap-5 lg:grid-cols-2"><Input label="Eyebrow" value={content.method.eyebrow} onChange={v=>field("method","eyebrow",v)}/><Input label="Título" value={content.method.title} onChange={v=>field("method","title",v)}/><Input label="Destaque" value={content.method.highlight} onChange={v=>field("method","highlight",v)}/><TextArea label="Descrição" value={content.method.description} onChange={v=>field("method","description",v)}/></div></EditorCard>
 <EditorCard title="CTA final" subtitle="Última chamada para orçamento."><div className="grid gap-5 lg:grid-cols-3"><Input label="Título" value={content.finalCta.title} onChange={v=>field("finalCta","title",v)}/><Input label="Botão" value={content.finalCta.button} onChange={v=>field("finalCta","button",v)}/><TextArea label="Descrição" value={content.finalCta.description} onChange={v=>field("finalCta","description",v)}/></div></EditorCard>
 <button onClick={save} disabled={saving} className="rounded-lg bg-navy-900 px-6 py-3 text-sm font-semibold text-white">{saving?"Salvando…":"Salvar e publicar tudo"}</button></main></div>
}
function EditorCard({title,subtitle,children}:{title:string;subtitle:string;children:ReactNode}){return <section className="border border-stone-200 bg-white p-6 shadow-sm sm:p-8"><div className="mb-7 border-b border-stone-100 pb-5"><h2 className="text-xl font-semibold text-navy-950">{title}</h2><p className="mt-1 text-sm text-stone-500">{subtitle}</p></div>{children}</section>}
function Input({label,value,onChange,type="text"}:{label:string;value:string;onChange:(v:string)=>void;type?:string}){return <label className="block"><span className="mb-2 block text-xs font-semibold uppercase tracking-wider text-stone-500">{label}</span><input type={type} value={value} onChange={e=>onChange(e.target.value)} className="h-11 w-full rounded-lg border border-stone-300 bg-white px-3 text-sm text-navy-900 outline-none focus:border-navy-700"/></label>}
function TextArea({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <label className="block"><span className="mb-2 block text-xs font-semibold uppercase tracking-wider text-stone-500">{label}</span><textarea value={value} onChange={e=>onChange(e.target.value)} rows={4} className="w-full rounded-lg border border-stone-300 bg-white p-3 text-sm text-navy-900 outline-none focus:border-navy-700"/></label>}
function ColorInput({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){const safe=/^#[0-9A-Fa-f]{6}$/.test(value)?value:"#0B3A64";return <label className="block"><span className="mb-2 block text-xs font-semibold uppercase tracking-wider text-stone-500">{label}</span><div className="flex gap-3"><input type="color" value={safe} onChange={e=>onChange(e.target.value.toUpperCase())} className="h-11 w-14 rounded border border-stone-300 p-1"/><input value={value} onChange={e=>onChange(e.target.value.toUpperCase())} className="h-11 flex-1 rounded-lg border border-stone-300 px-3 font-mono text-sm"/></div></label>}