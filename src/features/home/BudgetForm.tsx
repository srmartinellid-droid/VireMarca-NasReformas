"use client";
import { useState } from "react";
import { SERVICES } from "@/lib/site";
import { buildWhatsAppUrl } from "@/lib/utils";
export function BudgetModal({source="cta",whatsapp,triggerLabel="Solicitar orçamento"}:{source?:string;whatsapp:string;triggerLabel?:string}){const[open,setOpen]=useState(false);return <><button type="button" onClick={()=>setOpen(true)} className="inline-flex items-center justify-center rounded-lg bg-accent-500 px-6 py-3 text-sm font-semibold text-white shadow-sm transition hover:brightness-95">{triggerLabel}</button>{open&&<BudgetDialog source={source} whatsapp={whatsapp} onClose={()=>setOpen(false)}/>}</>}
function BudgetDialog({source,whatsapp,onClose}:{source:string;whatsapp:string;onClose:()=>void}){const[name,setName]=useState(""),[phone,setPhone]=useState(""),[service,setService]=useState(""),[message,setMessage]=useState(""),[website,setWebsite]=useState(""),[status,setStatus]=useState(""),[busy,setBusy]=useState(false);async function submit(event:React.FormEvent){event.preventDefault();if(busy)return;setBusy(true);setStatus("");const text="Olá, gostaria de solicitar um orçamento.\nNome: "+name+"\nTelefone: "+phone+"\nServiço: "+service+(message?"\nMensagem: "+message:"");const url=buildWhatsAppUrl(whatsapp,text);const popup=window.open(url,"_blank","noopener,noreferrer");try{const response=await fetch("/api/leads",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name,phone,service,message,source,website})});if(!response.ok)setStatus("Seu WhatsApp foi aberto. Não conseguimos registrar o pedido no painel, mas o contato segue normal.");else{setStatus("WhatsApp aberto. Obrigado pelo contato!");setTimeout(onClose,900)}}catch{setStatus("Seu WhatsApp foi aberto. O registro no painel não respondeu.")}finally{setBusy(false);if(!popup)window.location.href=url}}return <div className="fixed inset-0 z-[100] grid place-items-center bg-black/55 p-4" role="dialog" aria-modal="true" aria-labelledby="budget-title" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><div className="max-h-[92svh] w-full max-w-xl overflow-auto rounded-2xl bg-white p-6 text-navy-950 shadow-2xl sm:p-8"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-accent-600">Orçamento</p><h2 id="budget-title" className="mt-2 text-2xl font-semibold">Conte um pouco sobre sua obra</h2></div><button type="button" onClick={onClose} aria-label="Fechar" className="grid h-9 w-9 place-items-center rounded-full border border-stone-200 text-stone-500">×</button></div><form onSubmit={submit} className="mt-7 space-y-5"><label><span className="mb-2 block text-xs font-bold uppercase tracking-wider text-stone-500">Nome</span><input required maxLength={120} value={name} onChange={e=>setName(e.target.value)} className="h-12 w-full rounded-lg border border-stone-300 px-3"/></label><label><span className="mb-2 block text-xs font-bold uppercase tracking-wider text-stone-500">Telefone / WhatsApp</span><input required inputMode="tel" maxLength={20} value={phone} onChange={e=>setPhone(e.target.value)} className="h-12 w-full rounded-lg border border-stone-300 px-3"/></label><label><span className="mb-2 block text-xs font-bold uppercase tracking-wider text-stone-500">Serviço</span><select required value={service} onChange={e=>setService(e.target.value)} className="h-12 w-full rounded-lg border border-stone-300 bg-white px-3"><option value="">Selecione</option>{SERVICES.map(item=><option key={item.slug} value={item.title}>{item.title}</option>)}</select></label><label><span className="mb-2 block text-xs font-bold uppercase tracking-wider text-stone-500">Mensagem <span className="font-normal normal-case text-stone-400">(opcional)</span></span><textarea maxLength={1000} rows={4} value={message} onChange={e=>setMessage(e.target.value)} className="w-full rounded-lg border border-stone-300 p-3"/></label><label className="hidden" aria-hidden="true">Website<input tabIndex={-1} autoComplete="off" value={website} onChange={e=>setWebsite(e.target.value)}/></label><p className="text-xs leading-5 text-stone-500">Seus dados são usados só para responder ao seu pedido de orçamento. <a href="/privacidade" target="_blank" className="underline">Privacidade</a>.</p>{status&&<p className="rounded-lg bg-stone-100 px-3 py-2 text-sm text-stone-700">{status}</p>}<button type="submit" disabled={busy} className="w-full rounded-lg bg-navy-900 px-5 py-3.5 text-sm font-semibold text-white disabled:opacity-60">{busy?"Enviando…":"Continuar pelo WhatsApp ↗"}</button></form></div></div>}
